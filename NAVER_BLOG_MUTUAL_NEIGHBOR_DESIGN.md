# 네이버 블로그 서로이웃/친구추가 운영 보조 프로그램 설계(코드 작성 전)

> 목적: 사용자가 지정한 카테고리의 블로거를 탐색하고, 정책/리스크를 준수하면서 서로이웃 신청 및 친구추가(관계 형성)를 **운영 보조**하는 로직을 설계한다.

---

## 1. 목표와 범위

### 1.1 목표
- 입력된 카테고리(키워드/주제/블로그 분류) 기반으로 후보 블로그를 수집한다.
- 필터링 규칙(활동성, 품질, 중복 여부, 차단 위험 등)으로 신청 대상을 선정한다.
- 무리한 자동 반복 대신, 저빈도·사람 검수 중심의 실행 절차로 계정/블로그 품질 리스크를 낮춘다.
- 실행 로그/결과 리포트를 남겨 운영자가 검수 및 재시도할 수 있게 한다.

### 1.2 비목표(이번 설계에서 제외)
- CAPTCHA 우회, 인증 우회, 보안장치 무력화.
- 계정 대량 생성/대량 스팸형 신청.
- 플랫폼 약관 위반을 목적으로 한 은닉/우회 로직.
- 검증 로직 회피(탐지 우회) 목적의 패턴 위장.

---

## 2. 상위 아키텍처

1) **입력/설정 레이어**  
- 사용자 설정: 계정, 대상 카테고리, 1일 상한, 신청 메시지 템플릿, 금지 키워드.

2) **수집 레이어(Discovery)**  
- 카테고리/검색 결과/이웃 네트워크에서 후보 프로필 URL 및 메타데이터 수집.

3) **평가 레이어(Scoring & Filtering)**  
- 중복/차단/휴면/저품질 계정 제외.
- 점수 기반 우선순위 산정.

4) **실행 레이어(Action Executor)**  
- 프로필 방문 → 서로이웃 가능 여부 확인 → 신청 메시지 생성/검수 → 신청 수행.
- 실패 타입 분류(권한제한, 일시오류, UI변경 등).

5) **저장/상태 레이어(State & Audit)**  
- 후보/시도/성공/실패/재시도 큐 상태를 DB에 저장.
- 민감 런타임 데이터(쿠키/세션)와 운영 데이터 분리 저장.

6) **운영 레이어(Ops)**  
- 일일 리밋, 시간대 분산, 계정 건강도 모니터링, 수동 승인 모드.

---

## 2.1 플랫폼 신뢰/블로그 지수 보호 원칙(핵심)

- **우회 금지 원칙**: 네이버 검증/제한 로직을 피하기 위한 설계는 하지 않는다.
- **품질 우선 원칙**: 신청 수량보다 수락률/상호작용 품질을 KPI로 삼는다.
- **사람 승인 기본값**: 자동 제출이 아니라 “후보 추천 + 승인 후 실행”을 기본 모드로 한다.
- **저빈도 운영**: 일괄 대량 요청 대신 보수적인 상한과 충분한 간격을 유지한다.
- **콘텐츠 연동**: 최근 발행 글 주제와 연관된 대상만 신청하여 무관한 접근을 줄인다.

---

## 3. 핵심 도메인 모델

- **Campaign**: 특정 카테고리/기간/목표량을 가진 작업 단위.
- **TargetProfile**: 블로그 URL, 닉네임, 카테고리 적합도, 최근 활동일, 상태.
- **ActionAttempt**: 시도 시각, 액션 종류(방문/신청), 결과 코드, 스크린샷/로그 경로.
- **RelationshipState**: 미시도/신청완료/수락/거절/차단의 상태 머신.
- **AccountHealth**: 경고 횟수, 연속 실패율, 일일 액션 수, 쿨다운 필요 여부.

---

## 4. 처리 흐름(End-to-End)

### 4.1 사전 준비
- 설정 검증(필수값, 일일 상한, 메시지 템플릿 길이).
- 계정 로그인 상태 점검.
- 금일 잔여 쿼터 계산.

### 4.2 후보 수집
- 카테고리/키워드별 검색 결과 페이지 순회.
- 후보를 표준 스키마로 정규화(프로필ID, URL, 수집경로, 수집시각).
- 중복 URL/동일 프로필 병합.

### 4.3 후보 평가
- 하드 필터:
  - 이미 신청한 대상 제외.
  - 최근 n일 내 실패 반복 대상 제외.
  - 비공개/접근불가 프로필 제외.
- 소프트 스코어:
  - 최근 게시글 활동성
  - 카테고리 연관도
  - 상호 관심 가능성(공통 태그/주제)
- 상위 점수부터 실행 큐 적재.

### 4.4 신청 실행
- 각 타겟별 안전 절차:
  1. 프로필 방문 및 버튼 가용성 확인.
  2. 신청 텍스트 생성(템플릿 + 개인화 변수).
  3. 최종 검수 규칙(금칙어, 과도 반복문구, 길이).
  4. 운영자 승인(수동 확인) 후 신청 클릭/제출.
  5. 결과 상태 저장(성공/실패 코드).

### 4.5 사후 처리
- 성공: RelationshipState=신청완료, 재접촉 금지 기간 설정.
- 실패: 오류 유형별 재시도 정책 반영.
- 배치 종료 리포트 생성(성공률, 실패 상위 원인, 다음 실행 추천량).

---

## 5. 상태 머신 설계

`NEW -> QUEUED -> VISITED -> REQUESTED -> (ACCEPTED | REJECTED | EXPIRED)`

- `FAILED_TEMP` (네트워크/UI 일시 문제): 지수 백오프로 재시도.
- `FAILED_POLICY` (권한/제한/차단 징후): 즉시 중단 및 수동 검토.
- 전이는 모두 이벤트 로그로 감사 가능하게 저장.

---

## 6. 레이트 리밋/안전 운용 로직

- **일일 총량 제한**: 초기에는 매우 낮은 상한(예: 5~15)으로 시작해 점진 조정.
- **버스트 방지**: 짧은 시간 집중 요청 금지(고정 최소 대기 + 수동 검토 시간 포함).
- **연속 실패 차단기**: 연속 실패 임계치 초과 시 자동 중지.
- **쿨다운 정책**: 경고/제한 징후 발생 시 24~72시간 정지.
- **수동 승인 모드**: 점수가 애매한 타겟은 사람 승인 후 실행.

---

## 6.1 블로그 지수 보호용 운영 가드레일

- **일일 신규 신청 비율 제한**: 총 활동 대비 신청 액션 비율 상한 설정.
- **상호작용 선행 규칙**: 가능하면 먼저 공감/댓글 등 정상 상호작용 후 신청.
- **메시지 중복률 관리**: 템플릿 반복률을 낮추고, 카테고리 맥락 기반으로 작성.
- **거절/무응답 누적 시 축소 운용**: 일정 기간 성공률 저하 시 자동 감산.
- **휴면 계정 보호**: 장기간 비활동 타겟에는 신청하지 않음.
- **분기별 정책 점검**: 약관/정책 변경 시 룰셋 업데이트.

---

## 7. 보안 설계(질문에서 제시한 원칙 반영)

### 7.1 입력값 검증
- `identity_id` 등 경로/키로 쓰이는 값은 sanitize 후만 사용.
- 규칙: 빈 값 금지, 허용문자(영문/숫자/_/-), 길이 제한(예: 64).
- 원본 raw 입력은 로깅/저장 시 마스킹.

### 7.2 데이터 분리 저장
- **민감 런타임 데이터**(쿠키/세션/브라우저 프로필)는 VCS 추적 금지 경로에 저장.
- 운영 DB(시도 이력/점수/리포트)와 물리적으로 분리.

### 7.3 예외 처리 원칙
- 광범위 `except` 금지.
- 예상 가능한 예외만 명시 처리하고, 나머지는 실패로 올려 원인 파악 가능하게.

### 7.4 감사/탐지
- 모든 신청 액션에 request_id 부여.
- 실패 유형 통계, 비정상 패턴(짧은 간격 반복 등) 감지.

---

## 8. 품질/운영 진단 로직

- 신청 대비 수락률, 거절률, 무응답률을 핵심 지표로 모니터링.
- 과도한 신청 집중 구간/낮은 품질 타겟 비율을 탐지해 운영량 자동 축소.
- 주간 리포트: 카테고리별 성공률, 거절률, 제한 징후.

---

## 9. 실패 대응 시나리오

- **UI 구조 변경**: 셀렉터 검증 실패 시 즉시 중단, “UI_CHANGED” 코드 저장.
- **인증 만료**: 재로그인 필요 상태로 전환, 자동 실행 중단.
- **요청 제한/차단 징후**: 계정 건강도 하향, 장기 쿨다운 및 수동 점검.
- **데이터 손상(JSON 파싱 실패 등)**: 백업 복구 후 해당 배치 스킵.

---

## 10. 배포 전 체크리스트(코드 작성 전 합의용)

1. 약관/정책 준수 범위를 문서로 정의했는가?
2. 카테고리 선정 기준/금지 대상 기준이 명확한가?
3. 일일 상한/쿨다운/중단 조건이 설정됐는가?
4. 민감 데이터 경로가 VCS ignore 처리됐는가?
5. 로그에 PII/토큰/쿠키가 남지 않도록 마스킹 되는가?
6. 수동 승인 모드와 비상정지 스위치가 있는가?
7. KPI(성공률/거절률/제한율) 기준치가 정의됐는가?
8. “검증 로직 우회 금지”와 “사람 승인 기본” 원칙이 운영 정책에 반영됐는가?
9. 블로그 지수 악화 징후(거절률 상승, 무응답 누적) 발생 시 감산/중지 조건이 정의됐는가?

---

## 11. 구현 우선순위 제안

- **Phase 1 (MVP)**: 수집/중복제거/수동 승인 기반 신청/기본 리포트.
- **Phase 2**: 점수화 고도화, 자동 큐, 재시도 정책.
- **Phase 3**: 건강도 모델, 이상패턴 탐지, 운영 대시보드.

이 설계를 기준으로 다음 단계에서 화면/모듈 단위 상세 명세(API, 테이블 스키마, 이벤트 코드 체계)를 확정하면 코드 구현으로 넘어갈 수 있다.


---

## 13. 바이브코딩(코드 작성 직전) 추가 고려사항 체크리스트

> 빠르게 구현을 시작하더라도, 품질/정책/운영 리스크를 줄이기 위해 최소한 아래 항목은 먼저 확정한다.

### 13.1 제품/운영 경계 확정
- 이번 스프린트의 자동화 범위를 “추천/보조”와 “자동 실행”으로 구분하고, 기본값을 수동 승인 모드로 둔다.
- 기능 토글(예: `AUTO_REQUEST_ENABLED=false`)을 기본값으로 두고, 운영자가 명시적으로 켤 때만 자동 실행한다.
- 약관/정책 리스크가 높은 동작은 코드 레벨에서 비활성 처리하고, 문서에 이유를 남긴다.

### 13.2 도메인 이벤트/상태코드 먼저 고정
- 상태 전이 코드를 먼저 고정한다: `NEW`, `QUEUED`, `APPROVED`, `REQUESTED`, `FAILED_TEMP`, `FAILED_POLICY`.
- 실패 코드를 표준화한다: `AUTH_REQUIRED`, `UI_CHANGED`, `RATE_LIMITED`, `POLICY_BLOCKED`.
- 이벤트 로그 스키마를 고정한다: `request_id`, `campaign_id`, `profile_id`, `action`, `result_code`, `ts`.

### 13.3 데이터 스키마/저장소 규칙
- 런타임 민감데이터(세션/쿠키)와 운영데이터(DB/리포트)를 분리 저장한다.
- 스키마 마이그레이션 전략을 정한다(예: `schema_version` + startup check).
- 재처리 가능한 원본(후보 수집 결과)과 파생 데이터(점수/결정 결과)를 분리 저장한다.

### 13.4 입력 검증/가드레일
- `identity_id`, `campaign_name`, `category`는 허용 문자/길이 제한을 명시한다.
- 일일 상한은 설정값이 있더라도 시스템 하드캡(100)을 넘지 않도록 강제한다.
- 실행 전 프리체크를 공통 함수로 두고, 실패 시 즉시 종료 + 사용자에게 원인 코드 제공.

### 13.5 관측성(Observability) 최소 세트
- 구조화 로그(JSON)로 기록하고, 민감정보 마스킹 규칙을 적용한다.
- 배치 단위 메트릭을 기본 제공한다: 시도 수, 성공 수, 거절률, 무응답률, 중단 사유.
- 운영자가 보는 일일 요약 리포트 템플릿(텍스트/JSON)을 먼저 만든다.

### 13.6 테스트 전략(바이브코딩용 최소 안전망)
- 단위테스트 우선순위: sanitize, 점수화, 중단정책(한도/거절률/무응답률).
- 시뮬레이션 테스트: “후보 300명 입력 → 필터링/큐잉 → 최대 100건 제한” 시나리오.
- 회귀 방지용 골든 케이스를 둔다(동일 입력에 동일 우선순위/결정이 나오는지).

### 13.7 UI/셀렉터 변경 내성
- UI 자동화는 셀렉터 다중화(1순위/2순위)와 실패시 `UI_CHANGED` 분기 처리.
- DOM 의존 코드는 어댑터 계층으로 분리해 변경 영향을 국소화한다.
- 실패 스크린샷/HTML 스냅샷 저장 규칙을 정해 디버깅 시간을 줄인다.

### 13.8 배포/운영 안전장치
- 킬스위치(전역 중지 플래그)와 쿨다운(예: 24h) 정책을 환경설정으로 노출한다.
- 스케줄러는 시간대 분산(세션당 25건 이하) 정책을 기본으로 적용한다.
- 롤백 플랜: 이전 안정 버전으로 1커맨드 복귀 가능한 배포 스크립트 준비.

### 13.9 개발 시작 전 최종 확인 질문(5개)
1. 이번 구현에서 “완전 자동”이 아니라 “운영자 승인 우선”이 코드로 강제되는가?
2. 하루 100건 하드캡이 어떤 경로로도 우회되지 않게 막았는가?
3. 중단 트리거 발생 시 남은 큐가 자동 중지되는가?
4. 장애 발생 시 원인 코드만 보고도 재현/조치가 가능한가?
5. 정책 변경 시 설정만 바꿔도 운영 규칙을 업데이트할 수 있는가?
